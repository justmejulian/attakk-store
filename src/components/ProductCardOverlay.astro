---
interface Props {
  title: string;
  imageUrl: string;
}

const { title, imageUrl } = Astro.props as Props;
---

<div
  class="absolute flex h-full w-full translate-y-0 transform flex-col items-start justify-end overflow-hidden bg-black transition-transform duration-300 group-focus-within:translate-y-full group-hover:translate-y-full"
>
  <img
    src={imageUrl}
    alt={title}
    class="absolute z-10 h-full w-full object-cover opacity-30"
  />

  <div
    class="text-primary z-10 flex flex-col items-center justify-center gap-2 p-6"
  >
    <!-- {title} > -->
    <div>
      {title}
    </div>
    <div>⌄</div>
  </div>
</div>
